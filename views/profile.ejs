<%- include('partials/header') -%>
<main>
<div class="container">
      <div class="">
          <div>
            <p>Welcome to:<strong> Let's Do Korean</strong></p>
            <p>To see your progress please click on the korean flag icon on the top right of the screen</p>
          </div>
            <button class="sectionsBtn" href="/sections" role="button"><span class="text">Go to study sections</span></button>
          <div class="mt-5">
            <p>After completing a section it will be listed below (a passing grade is 60%)</p>
            <span>열심히 공부하자! 화이팅!</span>
            <h2>Completed stages</h2>
            <ul>
            <% for (let i = results.length-1; i >= 0 ; i--){ %>
              <% const theNum = Number(results[i].test) %>
              <li style="list-style-type: none;">
                <span>Section: <%= review[0].sections[theNum] %></span>
                <span>Score: <%= results[i].score %>%</span>
              </li>
            <% } %>
          </ul>
          </div>
      </div>
      <div class="">
        <ul class="row list-unstyled">
        </ul>
      </div>  
    </div>
  </div>
</main>
<%- include('partials/footer') -%>